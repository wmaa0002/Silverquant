# A股量化交易系统依赖包
# 生成时间: 2026-04-10

# ============ 核心依赖 ============

# 数据库
duckdb>=0.9.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# ============ 数据源 ============

# A股市场数据 (三路冗余)
akshare>=1.11.0          # 备用数据源
baostock>=0.8.8          # 备用数据源
tushare>=1.3.0           # 主数据源 (需 TUSHARE_TOKEN)

# ============ 回测框架 ============

backtrader>=1.9.78

# ============ AI/LLM 集成 ============

langchain-core>=0.1.0     # LLM核心
pydantic>=2.0.0          # 数据验证
requests>=2.28.0         # HTTP请求

# ============ Web 框架 ============

flask>=3.0.0             # Web仪表板
werkzeug>=3.0.0         # Flask依赖

# ============ 可视化 ============

matplotlib>=3.7.0
seaborn>=0.12.0

# ============ 科学计算 ============

scipy>=1.11.0

# ============ 工具 ============

tqdm>=4.65.0             # 进度条
python-dateutil>=2.8.0   # 日期时间处理
pyyaml>=6.0              # YAML配置

# ============ 数据库工具 ============

sqlalchemy>=2.0.0        # SQL工具 (用于 scan_signals_v2.py)

# ============ 缓存 (可选) ============

# redis>=5.0.0            # 可选: Redis缓存 (agent_integration/cache/redis_cache.py)
                         # 代码有优雅降级, 不安装也可运行

# ============ JoinQuant 聚宽 (可选, 仅策略文件使用) ============
# 仅在使用 strategies/jukuan/ 目录下策略时需要
# pip install jqdata jqfactor
# jqdata
# jqfactor

# ============ 开发/测试工具 ============

pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0

# ============ 已废弃/未使用 (保留供参考) ============

# ta-lib>=0.4.28         # 技术分析库 (代码中未使用)
# scikit-learn>=1.3.0     # 机器学习 (代码中未使用)
# statsmodels>=0.14.0     # 统计分析 (代码中未使用)
# plotly>=5.15.0          # 交互图表 (代码中未使用)
